// js/config.js
// Central config — change API_BASE_URL for different environments.
// ponytail: no env-file parsing needed; this is a static site.

/** Base URL for backend API. Trailing slash omitted. */
export const API_BASE_URL = '/backend/api';

/** Event metadata — update for next edition here only */
export const EVENT = {
  name: 'INI RUN FEST 2026',
  date: '28 Juni 2026',
  location: 'AEON Mall Deltamas, Cikarang',
  categories: ['5K', '10K', 'Half Marathon'],
};
